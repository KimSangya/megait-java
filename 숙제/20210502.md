# 배열 추가문제.

## 하

### 하-1 : int형 6칸 짜리 배열을 생성하세요.

```java
public class Homework01 {
	public static void main(String[] args) {
		int[] a;
		a = new int[6];
		for(int b =0; b < a.length; ++b) {
			System.out.print(a[b]+" ");
		}
	}

}
```

###  하-2 : 다음 출력 결과를 예상하세요.

```java
int[] a = new int[2]; 
	System.out.println(a[0]); // 답 : 0
	System.out.println(a[1]); // 답 : 0 

	double[] b = new double[2];
	System.out.println(b[0]); // 답 : 0.0 
	System.out.println(b[1]); // 답 : 0.0 

	String[] c = new String[2];
	System.out.println(c[0]); // 답 : null
	System.out.println(c[1]); // 답 : null 


	char[] d = new char[2];
	System.out.println(d[0]); // 답 :  안나옴
	System.out.println(d[1]); // 답 :  안나옴 


	boolean[] e = new boolean[2];
	System.out.println(e[0]); // 답 : false 
	System.out.println(e[1]); // 답 : false 
```

### 하-3 , 하-4

```java
package com.day08.homework;

import java.util.Arrays;
import java.util.Scanner;

public class Homework05 {
	public static void main(String[] args) {
		
//	 하-3 : 사용===> 자에게 배열의 칸 개수를 입력 받고, 해당 정수의 크기만큼 정수형 배열을 생성하세요.
//		입력 : 3  ===> 결과 : [ 0, 0, 0 ] 
//		입력 : 5  결과 : [ 0, 0, 0, 0, 0 ] 
	Scanner sc = new Scanner(System.in);
	System.out.print("칸 개수:");
	int size = sc.nextInt();
	int[] x = new int[size];
	
//	 하-4 : (3)번에서 생성된 배열에 다음 기능을 추가하세요.
//		ㄱ. 0 ~ n-1 까지의 숫자를 배열에 순서대로 저장하세요.
//		ㄴ. 배열의 가장 마지막 원소부터 0번 원소까지 역순으로 출력하세요.
//		ㄷ. for문을 사용하여 배열에 저장된 실제 원소들을 역순으로 재배치 하세요. (난이도 중)
	
	// ㄱ. 
	for(int i = 0; i < x.length; ++i) {
		x[i] = i;
	}
	System.out.println("ㄱ. " + Arrays.toString(x));
	
	// ㄴ.
	System.out.print("ㄴ. ");
	for(int i = x.length-1; i >= 0; --i) {
		System.out.print(x[i] + " ");
	}
	System.out.println();
	
	// ㄴ. (또다른 방법)
	System.out.print("ㄴ. ");
	for(int i = 0; i < x.length; ++i) {
		System.out.print(x[x.length - i - 1] + " ");
	}
	System.out.println();
	
	// ㄷ.
	int tmp;
	for(int i = 0; i < x.length / 2; ++i) {
		tmp = x[i];
		x[i] = x[x.length - i - 1];
		x[x.length - i - 1] = tmp;
	}
	System.out.println(x[0]);
	System.out.println("ㄷ. " + Arrays.toString(x));
	
	
	}
}
```

### 하-5 : 사용자에게 배열의 칸 개수를 입력 받고, 해당 정수의 크기만큼 String형 배열을 생성하고

​	**입력 : 3  ===> 결과 : [ null, null, null ]** 
​	**입력 : 5  ===> 결과 : [ null, null, null, null, null ]**
​	**사용자에게 입력받은 단어들을 순차적으로 배열에 저장하세요.**  

```java
import java.util.Arrays;
import java.util.Scanner;

public class homework04 {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		int a;
		System.out.println("배열의 칸 개수를 입력하세요.");
			a = sc.nextInt();
		String[] ans = new String[a];
		for(int i = 0; i <ans.length; ++i) {
				System.out.println("문자열 : ");
				ans[i] = sc.next();
		}
		System.out.println(Arrays.toString(ans));
	}
}
```

### 중-1 "OOXXOXXOOO"와 같은 OX퀴즈의 결과가 있다. O는 문제를 맞은 것이고, X는 문제를 틀린 것이다. 문제를 맞은 경우 그 문제의 점수는 그 문제까지 연속된 O의 개수가 된다. 예를 들어, 10번 문제의 점수는 3이 된다.

"OOXXOXXOOO"의 점수는 1+2+0+0+1+0+0+1+2+3 = 10점이다.

OX퀴즈의 결과가 주어졌을 때, 점수를 구하는 프로그램을 작성하시오.

```java
import java.util.Scanner;
public class Homework01 {
	public static void main(String[] args) {
		String[] a = {"O","O","X","X","O","X","X","O","O","O"};
		Scanner sc = new Scanner(System.in);
		int i;
		int c = 0;
		int sum = 0;
		String b;
		
		for(i = 0; i < a.length; ++i) {
				System.out.println((i+1) + "번째 문제의 정답은?");
				b = sc.next();
			if (b.equals(a[i])) {
				System.out.println("정답!");
					c = 0;
				}
			else {
				System.out.println("땡.."); 
				 	c+=1;
				 	sum+=c;
			}
		}
		System.out.println("당신의 최종 점수는" + sum + "점입니다~");
	}
}

```

### 중-2 

==>  int[] dates = {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31}; 
			1) dates 배열을 활용하여 1/1일부터 사용자에게 입력 받은 월/일 까지 며칠이 소요되는지 출력하세요.
		   단, 사용자에게 해는 따로 입력받지 않기때문에 윤년은 없다고 가정합니다.

예) 월 : 12   일 : 31  ==> 364일 소요!
		    월 : 4    일 : 12   ==> 101일
		원리) 4/12 의 결과를 계산하려면
		    1 / 1 ~ 1 / 31  => 31 (dates[0]) 
		    2 / 1 ~ 2 / 28  => 28 (dates[1])
		    3 / 1 ~ 3 / 31  => 31 (dates[2])
        	4 / 1 ~ 4 / 12  => 12 (사용자가 입력한 일)
		 => 31 + 28 + 31 + 12 - 1 = 101일 

2) 시작월/일과 목표 월/일 을 각각 입력 받고 d-day 계산기를 만드세요.
   단, year는 입력받지 않기때문에 d-day의 최댓값은 364일로 가정합니다.
   예) 시작 : 9/26  목표 : 11/23  ==> 결과 : d-day 58 !!!
       시작 : 1/1 목표 : 12/31  ==> 결과 : d-day 364 !!!
       시작 : 12/31 목표 : 1/1  ==> 결과 : d-day 1 !!!
       시작 : 4/12 목표 : 4/11  ==> 결과 : d-day 364 !!!
   원리)
	start : 1/1 ~ 시작 월/일까지 며칠이 소요되는지 계산합니다. 
	end : 1/1 ~ 목표 월/일까지 며칠이 소요되는지 계산합니다. 
	end-start 를 합니다. 
	이때 음수가 나온다면 목표일이 시작일보다 앞서있다는 의미입니다. (즉 목표일이 이듬해)
	이 경우 +365를 하면 됩니다.

##### 1)

```java
public class Homework02 {
	public static void main(String[] args) {
		int a = 0;
		int[] dates = { 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31 };
		Scanner sc = new Scanner(System.in);
		System.out.println("월을 입력하세요.");
		int month = sc.nextInt();
		System.out.println("일을 입력하세요.");
		int day = sc.nextInt();
		for (int n = 0; n < (month - 1); ++n) {
			a += dates[n];
		}
		a += day - 1;
		System.out.println("1/1 ~ " + month + "/" + day + ":" + a);

	}
}
```

##### 2)

```java
import java.util.Scanner;

public class Homework01 {
	public static void main(String[] args) {

		int a = 0;
		int b = 0;
		int[] dates = { 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31 };
		Scanner sc = new Scanner(System.in);
		System.out.println("월을 입력하세요.");
		int month1 = sc.nextInt();
		System.out.println("일을 입력하세요.");
		int day1 = sc.nextInt();
		System.out.println("디데이 월을 입력하세요.");
		int month2 = sc.nextInt();
		System.out.println("일을 입력하세요.");
		int day2 = sc.nextInt();
		for (int n = 0; n < (month1 - 1); ++n) {
			a += dates[n];
		}
		a += day1 - 1;
		for (int n = 0; n < (month2 - 1); ++n) {
			b += dates[n];
		}
		b += day2;

		if (b > a) {
			System.out.println(month1 + "/" + day1 + " ~ " + month2 + "/" + day2 + ":" + (b - a));
		} else {
			System.out.println(month1 + "/" + day1 + " ~ " + month2 + "/" + day2 + ":" + (b - a + 365));
		}
	}
}
```



### 중3

##### 1,2)

```java
public class Homework03 {
	public static void main(String[] args) {
		int i;
		int j;
		int k;
		int h = 0;
		String names;
		String[] a = { "김", "이", "박", "최", "한" };
		String[] b = new String[7];
		System.out.println("보기에 제시된 이름들을 입력하세요.");
		System.out.println("피카츄" + " 라이츄" + " 파이리" + " 꼬부기" + " 버터풀" + " 야도란" + " 피죤투");
		Scanner sc = new Scanner(System.in);
		for (i = 0; i < b.length; ++i) {
			b[i] = sc.next();
		}
    //1번
		for (j = 0; j < 10; ++j) {
			h = (int) (Math.random() * 5) + 1;
			k = (int) (Math.random() * 7) + 1;
			System.out.println((j + 1) + "번째 이름 " + a[h - 1] + b[k - 1]);
		}
	//2번
		for (int p = 0; p < a.length; ++p) {

			for (int l = 0; l < b.length; ++l) {
				System.out.println(a[p] + b[l]);
			}
		}

	}

}
```
